#PROMPT:

Take on the role of an experienced front-end developer.
I need a Stopwatch and Countdown Timer built with vanilla HTML, CSS, and JavaScript, using only two files: index.html and script.js.

You’ll receive an image reference showing the desired visual style — match its layout, colors, and typography as closely as possible (no external assets or libraries).

Requirements:

Two modes: Stopwatch and Countdown, toggleable.

Stopwatch: start, pause/resume, reset, and lap list.

Countdown: input time (HH:MM:SS), presets (e.g. 30s, 1m, 5m), start/pause/reset, and visual + audio alert when finished.

Accurate timing using performance.now() and requestAnimationFrame (not setInterval).

Monospaced digital display for stable layout.

Responsive and accessible (keyboard shortcuts, ARIA labels, visible focus states).

Save mode and last settings in localStorage.

Clean, readable, commented code.

No explanations, frameworks, or external dependencies.

Output format:
Reply with exactly two fenced code blocks in this order and with these filenames as info strings:

index.html

script.js

No extra text or explanations before or after the code blocks.

#CHATBOT: 

Chat GPT-5